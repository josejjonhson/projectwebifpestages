import { COLORS, FONT_SIZE } from "../../constants/theme.js";

export const styles = {
    header:{
        width: "100%",
        alignItems: "center"
    },
    logo:{
        width: 180,
        height: 230
    },
    title:{
        color: COLORS.dark_gray,
        fontSize: FONT_SIZE.md,
        textAlign: "center"
    }
}